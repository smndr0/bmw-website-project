import fs from 'fs';
// We need to run this from node, so no three.js loaders unless installed.
// actually let's just use gltf-pipeline or binary parser if needed, but it's hard.
// Is three installed? npm ls three
